import { VoiceConsole } from '@/components/VoiceConsole';

/** Minimal page shell for the lab. */
export default function App() {
  return (
    <main className="bg-background h-dvh w-full">
      <VoiceConsole />
    </main>
  );
}
