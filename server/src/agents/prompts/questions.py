"""Canonical interview question definitions."""

FIRST_BAD_VERSION_QUESTION = """\
You are given an API `bool isBadVersion(version)` that tells you whether a
version is bad. Versions are numbered from 1 through n. Once a version is bad,
every version after it is also bad. Find the first bad version while minimizing
the number of calls to the API.
"""
