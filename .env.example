# Local LiveKit dev server credentials (matches `livekit-server --dev`)
LIVEKIT_URL=ws://127.0.0.1:7880
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret
# Model provider keys (self-hosted plugins, not LiveKit Cloud)
DEEPGRAM_API_KEY=
GROQ_API_KEY=
